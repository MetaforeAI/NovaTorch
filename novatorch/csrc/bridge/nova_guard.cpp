#include "nova_guard.h"

// All methods are defined inline in the header.
// This TU exists so the build system always has a compilation unit for the
// guard module and to allow future non-inline additions.
